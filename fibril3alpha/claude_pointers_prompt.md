I think best in terms of single-threaded, linear pointer logic. I would like you to create a markdown document which details from start to finish how this codebase executes -- starting from when server.js gets called. I'm only interested in the data handling logic -- not the web initialization or asynchronous user input handling. I want the chain to begin at the instantiation of class objects and the initial function calls.

Begin the markdown document with a section containig filename and line number references for every Class Definition and every Function or Class Method. Then, draw a pointer diagram which shows how the user inputs trigger the calling of functions/methods, all the way until the DBN finishes running. 

Effectively, I want this to serve as a roadmap for me to understand how the codebase executes, so I can manually debug and make edits as I desire.
